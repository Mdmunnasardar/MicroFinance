# `collections-app/`

React UI for the **Collections** module (installments, dues, overdue, payment-list, report). Bundles three previously separate PHP areas (`installments/`, `due_system/`, `loans/payment.php`) under one React UI.

```bash
npm install
npm run dev
```

Existing PHP module (`../collections/`) is unchanged; this is the parallel React UI.