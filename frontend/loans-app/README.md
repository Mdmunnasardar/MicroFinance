# `loans-app/`

React UI for the **Loans** module (list, detail, add, edit, payment). Same stack as the rest of the apps.

```bash
npm install
npm run dev
```

Existing PHP module (`../loans/`) is unchanged; this app is the parallel React UI.