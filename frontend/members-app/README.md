# `members-app/`

React UI for the **Members** module. Built with Vite, React, TypeScript, Tailwind CSS and TanStack Query.

```bash
npm install
npm run dev
```

The existing PHP module in `../members/` remains unchanged. This app will consume JSON endpoints from the PHP backend.