# `savings-app/`

React UI for the **Savings** module (accounts, deposits, withdrawals, transactions). Same stack as the other apps.

```bash
npm install
npm run dev
```

The existing PHP module (`../savings/`) is unchanged; this is the parallel React UI.